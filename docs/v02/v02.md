# V0.2 — Decision-Support Basics, Matchup FDR, Baseline xPDK & Turn-Aware Lineup/Trade Optimization

> **Living milestone plan.**
>
> V0.2 is the first decision-support milestone built on top of the V0.1 data and rules foundation. V0.1 established official EuroLeague Fantasy (`fantaking-api.dunkest.com/api/v1`, `league_id=10`, plus `league_id=11` EuroCup compatibility) and EuroLeague Feeds (`feeds.incrowdsports.com/v2`) snapshot ingestion, SQLite storage, 11-unit squad validation (`4G, 4F, 2C, 1HC`), 5 court formations (`2-2-1`, `1-2-2`, `2-1-2`, `1-3-1`, `3-1-1`), and deterministic trade checks (`0%` sell-on tax / `100%` capital gain realization).
>
> **Current status:** Completed on branch `v02` (2026-09-22, ready for Pull Request to `main`). See [`docs/architecture.md`](../architecture.md) and [`docs/roadmap.md`](../roadmap.md).

---

## 1. Executive objective

The primary objective of V0.2 is:

> **Transform the deterministic V0.1 rules engine into a complete weekly EuroLeague Fantasy decision-support engine capable of evaluating squad financials, ranking multi-round fixtures, projecting player and Head Coach fantasy points ($\text{xPDK}$), optimizing Turn-1 / Turn-2 court lineups and captaincy via Real Option Value, and recommending legal 1-to-4 trade moves.**

```text
V0.1 Foundation (SQLite snapshots + deterministic rules + trade validator)
        ↓
V0.2 Workstreams:
  ├── P1: Detailed Squad & Capital-Gain Financial Report (`elf squad`)
  ├── P2: Position-Aware Matchup Difficulty (FDR) & Multi-Round Ticker (`elf fixtures`)
  ├── P3: Baseline Player (PIR + 10% Win Bonus) & Head Coach Margin xPDK (`expected_points.py`)
  ├── P4: Turn-Aware Lineup, 6th-Man, Bench & Captain Optimizer (`elf lineup`)
  └── P5: Legal 1-to-4 Trade Candidate Generator & Ranker (`elf suggest-trades`)
```

---

## 2. V0.2 priorities & workstreams

| Priority | Workstream | Target Module(s) | CLI Command | Importance |
| :--- | :--- | :--- | :--- | :--- |
| **P0** | Multi-round schedule & team strength ingestion | `api.py`, `storage.py`, `fixtures.py` | `elf update` | Critical |
| **P1** | Current-squad financial & capital-gain reporting | `squad_report.py` | `elf squad` | Critical |
| **P2** | Position-aware FDR & multi-round fixture ticker | `fixtures.py` | `elf fixtures` | Critical |
| **P3** | Baseline Player $\text{xPDK}$ & Head Coach margin model | `expected_points.py` | *(internal engine)* | Critical |
| **P4** | Turn-Aware (`T1`/`T2`) Lineup, 6th-Man & Captain Optimizer | `lineup.py` | `elf lineup` | Critical |
| **P5** | Automated 1-to-4 trade recommender | `suggest_transfers.py` | `elf suggest-trades` | Critical |
| **P6** | Independent validator gate & JSON output modes | `cli.py`, `rules.py` | `--json` across CLI | High |
| **P7** | Hermetic V0.2 unit & integration test suite | `tests/test_v02_*.py` | `pytest` | Critical |

---

## 3. Mathematical & domain specifications

### 3.1 Financial & Capital-Gain Accounting (`P1: squad_report.py`)

In EuroLeague Fantasy Challenge Classic Mode, there is **no 50% sell-on tax** (unlike FPL):
- **Purchase Price ($P_{\text{buy}, i}$)**: Stored in tenths of a Credit in `config/current_squad.json`.
- **Current Market Quotation ($P_{\text{mkt}, i}$)**: Fetched from the latest SQLite snapshot (`price_tenths`).
- **Selling Price ($P_{\text{sell}, i}$)**:
  $$P_{\text{sell}, i} = P_{\text{mkt}, i}$$
- **Unrealized Capital Gain ($\Delta \text{Cr}_i$)**:
  $$\Delta \text{Cr}_i = \frac{P_{\text{mkt}, i} - P_{\text{buy}, i}}{10.0}$$
- **Total Liquid Team Value ($V_{\text{team}}$)**:
  $$V_{\text{team}} = \frac{\text{Bank}_{\text{tenths}} + \sum_{i=1}^{11} P_{\text{sell}, i}}{10.0}$$

`elf squad` will report per-unit purchase price, current quotation, net capital gain (`+Cr` / `-Cr`), club concentration (`x / 6`), position distribution (`4G, 4F, 2C, 1HC`), available free trades (`0..4`), and next Unlimited Trade Window (`R7, R14, R19, R24, R29, R35`).

---

### 3.2 Multi-Round Fixture Ticker & Position-Specific FDR (`P2: fixtures.py`)

Each EuroLeague Round consists of **Turns (`T1`, `T2`, `T3`)** corresponding to game days (e.g. Tuesday vs Wednesday in double-round weeks, or Thursday vs Friday in single-round weeks).

1. **Team Strength Index ($S_t \in [0, 1]$)**:
   - Derived from team roster value ($\sum \text{top-8 player quotations}$), historical/current-season win rate, and net point differential from `feeds.incrowdsports.com/v2/competitions/{E|U}/seasons/{season}/games`.
2. **Win Probability & Expected Margin ($\mu_{\Delta}, \sigma_{\Delta}$)**:
   - For team $A$ vs opponent $B$ with home-court advantage $H \approx +3.8$ points:
     $$\mu_{\Delta} = \alpha \cdot (S_A - S_B) + H \cdot \mathbb{I}(\text{home}), \quad \sigma_{\Delta} \approx 11.5\text{ pts}$$
     $$P(\text{Win}_A) = \Phi\left(\frac{\mu_{\Delta}}{\sigma_{\Delta}}\right)$$
3. **Fixture Difficulty Rating ($\text{FDR} \in \{1, 2, 3, 4, 5\}$)**:
   - Maps opponent strength and venue into a transparent 1–5 scale (`1 = Easiest Home Matchup`, `5 = Elite EuroLeague Road Test`), with position multipliers ($\lambda_{\text{FDR}, p} \in [0.88, 1.12]$) for `G`, `F`, `C`, and `HC`.
4. **Turn Tagging (`T1`, `T2`, `T3`)**:
   - Every fixture cell in `elf fixtures --rounds 5` displays Opponent, Venue (`H`/`A`), Turn (`T1`/`T2`), and `FDR` (e.g., `vs RMB (H, T1, FDR 4)`).

---

### 3.3 Baseline Expected Fantasy Points ($\text{xPDK}$) Model (`P3: expected_points.py`)

#### A. Court Players (`G`, `F`, `C`)
EuroLeague Fantasy player score is **Modified PIR + 10% Team Win Bonus**:
$$\text{Score}_i = \text{PIR}_i \times \left(1 + 0.10 \cdot \mathbb{I}(\text{Team}_i \text{ Wins})\right)$$

For V0.2, the baseline expected fantasy points $\text{xPDK}_i$ for player $i$ in Round $r$ is:
$$\text{BasePIR}_i = w_{\text{season}} \cdot \text{AvgPDK}_i + (1 - w_{\text{season}}) \cdot \text{PricePrior}(\text{Cr}_i, \text{Status}_i)$$
where:
- $\text{PricePrior}(\text{Cr}_i, \text{Status}_i)$ anchors early-season projections (Round 1–4) using the player's official market `quotation` (`Cr` $\times 1.02$ for `starter`, $\times 0.78$ for `bench`) blended with `avg_fantasy_pts` and `last_match_pts` via empirical Bayes shrinkage ($w_{\text{season}} = \frac{\text{GP}}{\text{GP} + 4}$).
- **Availability & Depth-Chart Discount ($A_i$)**:
  $$A_i = P(\text{play}_i) \times \begin{cases} 1.00 & \text{if status} = \text{starter} \\ 0.85 & \text{if status} = \text{bench} \\ 0.00 & \text{if status} = \text{out / injured} \end{cases}$$
- **Matchup & Win-Bonus Multiplier**:
  $$\text{xPDK}_i = A_i \cdot \text{BasePIR}_i \cdot \lambda_{\text{FDR}}(\text{opp}, \text{venue}, \text{pos}_i) \cdot \left(1 + 0.10 \cdot P(\text{Win}_{\text{team}_i})\right)$$
- **Player Score Standard Deviation ($\sigma_i$)**:
  $$\sigma_i = \max\left(4.0, \, 0.45 \cdot \text{xPDK}_i\right)$$

#### B. Head Coaches (`HC`)
Head Coaches score discrete step points based on final margin $M = \text{Score}_{\text{team}} - \text{Score}_{\text{opp}}$:
- Win by $1\text{--}10$ (or OT): $+10$
- Win by $11\text{--}20$: $+20$
- Win by $21+$: $+25$
- Loss by $1\text{--}10$ (or OT): $-5$
- Loss by $11\text{--}20$: $-10$
- Loss by $21+$: $-20$

Using the normal margin distribution $M \sim \mathcal{N}(\mu_{\Delta}, \sigma_{\Delta}^2)$:
$$\text{xPDK}_{\text{HC}} = +25 \cdot P(M > 20.5) + 20 \cdot P(10.5 < M \le 20.5) + 10 \cdot P(0 < M \le 10.5) - 5 \cdot P(-10.5 \le M < 0) - 10 \cdot P(-20.5 \le M < -10.5) - 20 \cdot P(M < -20.5)$$

---

### 3.4 Turn-Aware Lineup & Captaincy Optimizer (`P4: lineup.py`)

In Classic Mode, all 11 squad members contribute to the round total:
- **5 Starters**: $1.0\times$ (with **1 Captain** at $2.0\times$, i.e., $+1.0\times$ bonus)
- **1 Sixth Man**: $1.0\times$
- **4 Bench Players**: $0.5\times$
- **1 Head Coach**: $1.0\times$

Without intra-round substitutions, the static expected points of a lineup assignment is:
$$\text{xP}_{\text{static}} = \text{xPDK}_{\text{HC}} + \text{xPDK}_{\text{Cap}} + \sum_{i \in \text{Starters} \cup \{\text{6th}\}} \text{xPDK}_i + 0.5 \sum_{j \in \text{Bench}_4} \text{xPDK}_j$$

#### The Turn-1 $\to$ Turn-2 Real Option Value ($\Delta \text{Option xP}$)
When a `1.0x` slot (Starter or Sixth Man) is assigned to a **Turn 1 (`T1`)** player $i$ while an eligible **Turn 2 (`T2`)** player $j$ (capable of forming a legal formation after substitution) is placed on the `0.5x` Bench:
- After `T1` finishes, the manager observes the **exact realized score $S_i$**.
- If $S_i < \text{xPDK}_j$, the manager substitutes $j$ in ($1.0\times$) and demotes $i$ to the bench ($0.5\times$).
- If $S_i \ge \text{xPDK}_j$, the manager keeps $i$ at $1.0\times$ and leaves $j$ at $0.5\times$.
- The **Option Value gain** over static scoring for pair $(i \in T_1, j \in T_2)$ is:
  $$\Delta \text{Option}_{\text{slot}}(i, j) = 0.5 \cdot \mathbb{E}\left[\max\left(0, \, \text{xPDK}_j - S_i\right)\right]$$
  Under $S_i \sim \mathcal{N}(\mu_i, \sigma_i^2)$, with $z = \frac{\mu_j - \mu_i}{\sigma_i}$:
  $$\mathbb{E}\left[\max(0, \mu_j - S_i)\right] = (\mu_j - \mu_i)\Phi(z) + \sigma_i \phi(z)$$
- Similarly, for **Captaincy (`2.0x`)**, assigning the `T1` armband to $c_1 \in T_1$ with a designated `T2` vice-captain $c_2 \in T_2$ yields an additional **Captain Option Value**:
  $$\Delta \text{Option}_{\text{Cap}}(c_1, c_2) = \mathbb{E}\left[\max\left(0, \, \mu_{c_2} - S_{c_1}\right)\right] = (\mu_{c_2} - \mu_{c_1})\Phi\left(\frac{\mu_{c_2} - \mu_{c_1}}{\sigma_{c_1}}\right) + \sigma_{c_1}\phi\left(\frac{\mu_{c_2} - \mu_{c_1}}{\sigma_{c_1}}\right)$$

`elf lineup` enumerates all legal assignments of the 10 court players into `(5 Starters in a legal formation, 1 Captain, 1 Sixth Man, 4 Bench)` + `1 Head Coach`, maximizes:
$$\text{Total Turn-Adjusted xPDK} = \text{xP}_{\text{static}} + \Delta \text{Option}_{\text{Cap}} + \Delta \text{Option}_{\text{slot}}$$
and passes the winning lineup through `rules.validate_court_lineup()` before returning it.

---

### 3.5 Automated 1-to-4 Trade Candidate Generator & Ranker (`P5: suggest_transfers.py`)

Given the manager's `CurrentSquadState` (`11` units, `bank_tenths`, `free_trades` $\in \{1, 2, 3, 4\}$):
1. Pre-filters candidate incoming players/coaches per position (`G`, `F`, `C`, `HC`) using Pareto dominance on `(price_tenths, turn_adjusted_xPDK)`.
2. Searches legal `k`-trade combinations ($k \in \{1, 2, 3, 4\}$) satisfying:
   - Position preservation (`4G, 4F, 2C, 1HC`)
   - Club limit ($\le 6$ players per club)
   - Affordability ($\text{Bank}_{\text{before}} + \sum P_{\text{sell, out}} - \sum P_{\text{buy, in}} \ge 0$)
3. Evaluates each candidate squad by running the `Turn-Aware Lineup Optimizer` (`lineup.py`) to compute the **net gain in Turn-Adjusted Roster xPDK ($\Delta \text{xPDK}$)**.
4. Verifies every recommended trade bundle via `transfers.validate_trades()` before surfacing it in `elf suggest-trades`.

---

## 4. Planned files & module layout for V0.2

### New modules in `src/euroleague_fantasy_manager/`
- `src/euroleague_fantasy_manager/squad_report.py` — Detailed 11-unit squad financial & capital-gain breakdown (`elf squad`).
- `src/euroleague_fantasy_manager/fixtures.py` — Multi-round schedule loader, team strength rating, position-specific FDR (`1..5`), and Turn (`T1/T2/T3`) ticker (`elf fixtures`).
- `src/euroleague_fantasy_manager/expected_points.py` — Baseline Player ($\text{xPDK}$) and Head Coach margin projection engine.
- `src/euroleague_fantasy_manager/lineup.py` — Turn-aware Starting 5 + 6th Man + 4 Bench + Captain (`T1->T2` Real Option Value) optimizer (`elf lineup`).
- `src/euroleague_fantasy_manager/suggest_transfers.py` — Legal `1..4` trade candidate generator and optimizer (`elf suggest-trades`).

### Modified modules
- `src/euroleague_fantasy_manager/api.py` & `storage.py` — Persist multi-round schedule metadata and official season game results for FDR & coach margin calculation.
- `src/euroleague_fantasy_manager/cli.py` — Wire `elf squad`, `elf fixtures`, `elf lineup` (aliases: `starting-five`, `captain`), and `elf suggest-trades` (alias: `suggest-transfers`).

### New test files in `tests/`
- `tests/test_v02_squad_and_fixtures.py`
- `tests/test_v02_expected_points_and_lineup.py`
- `tests/test_v02_suggest_trades.py`

---

## 5. V0.2 Exit Criteria (Release Gate for PR to `main`)

1. `elf squad` accurately computes purchase price, current quotation, `100%` selling price, unrealized capital gains, bank, total team value, and club/position quotas.
2. `elf fixtures --rounds 5` (and `--squad-only`) displays multi-round fixtures with Turn (`T1`/`T2`) tags and `1..5` FDR ratings.
3. `elf lineup` selects a deterministically legal Starting 5 formation (`2-2-1`, `1-2-2`, `2-1-2`, `1-3-1`, `3-1-1`), Captain (`2.0x`), Sixth Man (`1.0x`), 4 Bench (`0.5x`), and Head Coach (`1.0x`), explicitly reporting both `Static xPDK` and `Turn-Option xPDK` (`T1 -> T2` option bonus).
4. `elf suggest-trades --trades 1..4` generates ranked, legal trade bundles (players + Head Coach) verified by `validate_trades()`.
5. 100% of unit and integration tests pass (`pytest`).
